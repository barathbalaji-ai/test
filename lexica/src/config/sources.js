// Future live content sources (published Google Sheet CSV URLs).
// Empty URLs mean the bundled JSON seed renders — the site always works.
// Wire a sheet later and its rows are normalized into the same node shape.
export const SOURCES = {
  nodes: '',
  relationships: '',
}

// Company domain allowed through the launch gate.
export const ALLOWED_DOMAIN = 'freshworks.com'
