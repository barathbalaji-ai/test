{
  "name": "carta",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Carta — an editorial, motion-driven learning portal for Customer Support teams.",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run build:universe",
    "build:universe": "cd lexica && npm install --no-audit --no-fund && LEXICA_BASE=/universe/ npm run build && cd .. && rm -rf dist/universe && cp -r lexica/dist dist/universe",
    "preview": "vite preview"
  },
  "dependencies": {
    "framer-motion": "^11.3.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.0",
    "three": "^0.160.1"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.39",
    "tailwindcss": "^3.4.7",
    "vite": "^5.3.5"
  }
}
