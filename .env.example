# Carta needs no environment variables.
#
# The site is a pure static frontend. All content is read from published
# Google Sheet CSV URLs, which live in src/config/sources.js (not in env).
# There is no backend, database, or server-side secret.
#
# See CONTENT_GUIDE.md for how to wire the sheets.
